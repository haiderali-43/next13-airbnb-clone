import React from "react";
import { Listing, Reservation, User } from "@prisma/client";

const ListingCard = ({ currentUser = User, data = Listing }) => {
  return <div></div>;
};

export default ListingCard;
